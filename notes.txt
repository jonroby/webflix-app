Redis Cache and popular first pages, note I didn't set up ReJSON for heroku,
which means the value size is quite large (average of 6kb!).

Why Using list of lists, adding ref at top

Go through Slider and explain exactly why things were done as they were. Styling especially.

Render props

Throttling/Debounce

